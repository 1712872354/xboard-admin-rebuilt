import React, { useEffect, useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { Activity, ChevronLeft, ChevronRight, CircleHelp, Cpu, HardDrive, Languages, Moon, Pencil, Plus, Search, Server, Trash2, Users, X } from 'lucide-react';
import { machineApi, type Machine, type MachineNode } from './api/client';
import './styles.css';
type Status='online'|'offline'|'inactive'|'never';
const statusOf=(m:Machine):Status=>!m.is_active?'inactive':!m.last_seen_at?'never':Date.now()/1000-m.last_seen_at<300?'online':'offline';
const pct=(m:Machine)=>({cpu:m.load_status?.cpu??0,mem:m.load_status?.mem?.total?(m.load_status.mem.used/m.load_status.mem.total)*100:0,disk:m.load_status?.disk?.total?(m.load_status.disk.used/m.load_status.disk.total)*100:0});
const ago=(n?:number|null)=>{if(!n)return'-';const s=Math.max(0,Math.floor(Date.now()/1000-n));return s<60?`${s}s`:s<3600?`${Math.floor(s/60)}m`:s<86400?`${Math.floor(s/3600)}h`:`${Math.floor(s/86400)}d`};
const bytes=(n=0)=>n<1024**3?`${(n/1024**2).toFixed(0)} MB`:`${(n/1024**3).toFixed(2)} GB`;
function Header(){const[dark,setDark]=useState(false);useEffect(()=>document.documentElement.classList.toggle('dark',dark),[dark]);return <header className="topbar"><div className="brand">XBoard</div><div className="top-actions"><button className="icon-btn"><Search size={17}/></button><button className="icon-btn" onClick={()=>setDark(v=>!v)}><Moon size={17}/></button><button className="lang"><Languages size={15}/> CN</button><div className="avatar">A</div></div></header>}
function Overview({machines}:{machines:Machine[]}){const online=machines.filter(m=>statusOf(m)==='online').length;const offline=machines.filter(m=>['offline','never'].includes(statusOf(m))).length;const high=machines.filter(m=>{const p=pct(m);return p.cpu>=85||p.mem>=90||p.disk>=90}).length;const nodes=machines.reduce((n,m)=>n+(m.servers_count??0),0);const cards=[['total','TOTAL',machines.length,Server],['online','ONLINE',online,Activity],['offline','OFFLINE',offline,CircleHelp],['high','HIGH LOAD',high,Cpu],['nodes','NODES',nodes,Users]] as const;return <div className="overview">{cards.map(([k,l,v,I])=><div className="stat" key={k}><div><div className="stat-label">{l}</div><div className="stat-value">{v}</div></div><span className={`stat-icon ${k}`}><I size={15}/></span></div>)}</div>}
function Load({machine}:{machine:Machine}){const p=pct(machine);if(!machine.load_status)return <span className="muted">No data</span>;return <div className="load"><Metric label="CPU" value={p.cpu} icon={<Cpu size={11}/>}/><Metric label="MEM" value={p.mem} icon={<Users size={11}/>}/><div className="disk"><span><HardDrive size={11}/> DISK</span><span>{bytes(machine.load_status.disk.used)} / {bytes(machine.load_status.disk.total)}</span></div></div>}
function Metric({label,value,icon}:{label:string;value:number;icon:React.ReactNode}){return <div><div className="metric-head"><span>{icon}{label}</span><b>{value.toFixed(0)}%</b></div><div className="bar"><i style={{width:`${Math.min(value,100)}%`}}/></div></div>}
function MachineModal({machine,onClose,onSaved}:{machine?:Machine|null;onClose:()=>void;onSaved:()=>void}){const[name,setName]=useState(machine?.name??'');const[notes,setNotes]=useState(machine?.notes??'');const[saving,setSaving]=useState(false);const save=async()=>{if(!name.trim())return;setSaving(true);try{await machineApi.save({id:machine?.id,name:name.trim(),notes});onSaved();onClose()}finally{setSaving(false)}};return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><h3>{machine?'Edit machine':'New machine'}</h3><button className="icon-btn" onClick={onClose}><X size={17}/></button></div><label>Name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Machine name"/></label><label>Notes<textarea value={notes} onChange={e=>setNotes(e.target.value)} rows={4}/></label><div className="modal-actions"><button className="btn ghost" onClick={onClose}>Cancel</button><button className="btn primary" disabled={saving||!name.trim()} onClick={save}>{saving?'Saving…':'Save'}</button></div></div></div>}
function Detail({machine,onClose}:{machine:Machine;onClose:()=>void}){const[nodes,setNodes]=useState<MachineNode[]>([]);const[loading,setLoading]=useState(true);const[token,setToken]=useState<string>();const[command,setCommand]=useState<string>();useEffect(()=>{machineApi.nodes(machine.id).then(setNodes).finally(()=>setLoading(false))},[machine.id]);return <div className="modal-backdrop"><div className="modal wide"><div className="modal-head"><h3><Server size={18}/> {machine.name}</h3><button className="icon-btn" onClick={onClose}><X size={17}/></button></div><div className="detail-meta"><span>SID: {machine.id}</span><span className={`badge ${statusOf(machine)}`}>{statusOf(machine)}</span><span>CPU {pct(machine).cpu.toFixed(0)}%</span><span>Last seen: {ago(machine.last_seen_at)}</span><span>Nodes: {machine.servers_count}</span></div>{machine.notes&&<p className="muted">{machine.notes}</p>}<div className="detail-actions"><button className="btn" onClick={async()=>setToken((await machineApi.token(machine.id)).token)}>Get token</button><button className="btn" onClick={async()=>setCommand((await machineApi.installCommand(machine.id)).command)}>Install command</button></div>{token&&<pre className="code">{token}</pre>}{command&&<pre className="code">{command}</pre>}<h4>Associated nodes</h4>{loading?<p className="muted">Loading…</p>:nodes.length?<div className="node-list">{nodes.map(n=><div key={n.id}><b>#{n.id} {n.name}</b><span>{n.type}</span><span>{n.host}:{n.port}</span><span className={n.enabled?'enabled':'disabled'}>{n.enabled?'Enabled':'Disabled'}</span></div>)}</div>:<p className="muted">No nodes</p>}</div></div>}
function App(){const[machines,setMachines]=useState<Machine[]>([]);const[query,setQuery]=useState('');const[status,setStatus]=useState('');const[nodeState,setNodeState]=useState('');const[selected,setSelected]=useState<Machine|null>(null);const[editing,setEditing]=useState<Machine|null|undefined>();const[error,setError]=useState('');const reload=()=>machineApi.list().then(setMachines).catch(e=>setError(e.message));useEffect(reload,[]);const filtered=useMemo(()=>machines.filter(m=>{const q=query.toLowerCase(),s=statusOf(m),p=pct(m),high=p.cpu>=85||p.mem>=90||p.disk>=90;return(!q||`${m.name} ${m.notes??''} sid:${m.id}`.toLowerCase().includes(q))&&(!status||s===status)&&(!nodeState||(nodeState==='with_nodes'?m.servers_count>0:nodeState==='idle_nodes'?m.servers_count===0:high))}),[machines,query,status,nodeState]);const remove=async(m:Machine)=>{if(confirm(`Delete ${m.name}?`)){await machineApi.remove(m.id);reload()}};return <><Header/><main><div className="page-head"><div><h1>服务器管理</h1><p>管理服务器机器、节点关联和运行状态。</p></div><button className="btn primary" onClick={()=>setEditing(null)}><Plus size={16}/> 添加服务器</button></div><Overview machines={machines}/><section className="panel"><div className="toolbar"><div className="toolbar-left"><div className="search"><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="搜索服务器..."/></div><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">状态</option><option value="online">在线</option><option value="offline">离线</option><option value="inactive">停用</option><option value="never">未连接</option></select><select value={nodeState} onChange={e=>setNodeState(e.target.value)}><option value="">节点</option><option value="with_nodes">有节点</option><option value="idle_nodes">空闲</option><option value="high_load">高负载</option></select></div><div className="ratios">在线率: {machines.length?Math.round(machines.filter(m=>statusOf(m)==='online').length/machines.length*100):0}%　高负载: {machines.filter(m=>{const p=pct(m);return p.cpu>=85||p.mem>=90||p.disk>=90}).length}</div></div>{error&&<div className="error">{error}</div>}<div className="table-wrap"><table><thead><tr><th>服务器</th><th>状态</th><th>负载</th><th>节点</th><th>最后心跳</th><th>操作</th></tr></thead><tbody>{filtered.map(m=><tr key={m.id}><td><div className="machine"><span className="machine-icon"><Server size={16}/></span><div><div className="machine-name">{m.name}<i className={`dot ${statusOf(m)}`}/><small>SID: {m.id}</small></div>{m.notes&&<div className="muted ellipsis">{m.notes}</div>}</div></div></td><td><span className={`badge ${statusOf(m)}`}>{statusOf(m)}</span></td><td><Load machine={m}/></td><td><b>{m.servers_count}</b><span className="muted"> {m.servers_count?'nodes hosted':'nodes idle'}</span><br/><button className="btn small" onClick={()=>setSelected(m)}>详情</button></td><td><b>{ago(m.last_seen_at)}</b><div className="muted">{m.load_status?.updated_at?`last report: ${ago(m.load_status.updated_at)}`:'No data'}</div></td><td><div className="actions"><button className="icon-btn" onClick={()=>setSelected(m)}><Activity size={15}/></button><button className="icon-btn" onClick={()=>setEditing(m)}><Pencil size={15}/></button><button className="icon-btn danger" onClick={()=>remove(m)}><Trash2 size={15}/></button></div></td></tr>)}{!filtered.length&&<tr><td colSpan={6} className="empty">暂无数据</td></tr>}</tbody></table></div><div className="pagination"><span>共 {filtered.length} 台</span><div><button className="icon-btn"><ChevronLeft size={15}/></button><span>1 / 1</span><button className="icon-btn"><ChevronRight size={15}/></button></div></div></section></main>{editing!==undefined&&<MachineModal machine={editing} onClose={()=>setEditing(undefined)} onSaved={reload}/>} {selected&&<Detail machine={selected} onClose={()=>setSelected(null)}/>}</>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
